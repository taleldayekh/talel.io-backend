from talelio_backend.app_social.domain.post_model import AudienceModel, MediaModel, PostModel


def publish_post(platforms, post):
    # post = PostModel(platforms, content=post['content'], media=[MediaModel(**media) for media in post['media']], audience=AudienceModel(**post['audience']))

    print('here')
    print(post)

    pass
